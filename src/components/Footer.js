import React from 'react';

function Footer() {
    return (
        <footer>
            <h4>Will add images of github(w/link to profile) and image of linkedIn</h4>
        </footer>
    )
}

export default Footer;